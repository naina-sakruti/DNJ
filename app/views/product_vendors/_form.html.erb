<%= form_with(model: product_vendor, local: true) do |form| %>
  <% if product_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_vendor.errors.count, "error") %> prohibited this product_vendor from being saved:</h2>

      <ul>
      <% product_vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :vendor_id %>
    <%= form.text_field :vendor_id %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
